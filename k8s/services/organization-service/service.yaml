apiVersion: v1
kind: Service
metadata:
  name: organization-service
  namespace: patient-system
spec:
  selector:
    app: organization-service
  ports:
    - name: http
      port: 4005
      targetPort: 4005
  type: ClusterIP
