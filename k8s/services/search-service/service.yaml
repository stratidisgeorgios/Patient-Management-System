apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: patient-system
spec:
  selector:
    app: search-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
